module.exports = {
  apps: [{
    name: '{{ mcpo_service_name }}',
    script: '{{ ai_hub_base_path }}/venvs/mcpo/bin/mcpo',
    interpreter: 'none',  // Use script's shebang (Python), not Node.js
    args: '--port {{ mcpo_port }} --api-key {{ mcpo_api_key }} -e OPENAI_API_KEY={{ openai_api_key }} -e QDRANT_URL=http://localhost:6333 -e SQLITE_DB_PATH={{ ai_hub_base_path }}/data/memory.db -- node {{ mcp_memory_server_path }}',
    cwd: '{{ ai_hub_base_path }}',
    env: {
      NODE_ENV: 'production'
    },
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '500M',
    error_file: '{{ ai_hub_base_path }}/logs/mcpo-error.log',
    out_file: '{{ ai_hub_base_path }}/logs/mcpo-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z'
  }]
};
